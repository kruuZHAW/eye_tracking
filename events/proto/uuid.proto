syntax = "proto3";

import "configuration-editor-options.proto";

package tern.protobuf;

// Uuid
message Uuid
{
  option(tern.protobuf.aeronautical.excludeFromTree) = true;
  string uuid = 1 [(tern.protobuf.aeronautical.isUuid) = true];
}